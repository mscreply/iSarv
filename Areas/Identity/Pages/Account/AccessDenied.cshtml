@page
@model AccessDeniedModel
@{
    ViewData["Title"] = @Localizer.TextIgnoreCase("Access denied");
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@Localizer.TextIgnoreCase("You do not have access to this resource.")</p>
</header>
