@page
@model iSarv.Areas.Test.Pages.Holland.Question.DeleteModel

@{
    ViewData["Title"] = Localizer.Text("Delete");
}

<h1>@Localizer.TextIgnoreCase("Delete")</h1>

<h3>@Localizer.TextIgnoreCase("Are you sure you want to delete this?")</h3>
<div>
    <h4>@Localizer.TextIgnoreCase("Holland Test Question")</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.HollandTestQuestion.Statement)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.HollandTestQuestion.Statement)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.HollandTestQuestion.HollandPersonality)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.HollandTestQuestion.Type)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="HollandTestQuestion.Id" />
        <input type="submit" value="@Localizer.TextIgnoreCase("Delete")" class="btn btn-danger" /> 
        <a asp-page="./Index" class="btn btn-secondary">@Localizer.TextIgnoreCase("Back to List")</a>
    </form>
</div>
